# Contributing

Use synthetic fixtures, run `cd api && pytest -q`, and run the frontend type check before opening a pull request. Do not add credentials, real documents, generated `.env` files, or cloud state. Changes affecting security boundaries need a threat-model update and an ADR.
